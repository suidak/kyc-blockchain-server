var express = require('express');
var router = express.Router();
var kyc = require('../controllers/kycController');

// router.post('/', kyc.upload_file)
// router.get('/:fn', kyc.read_file)

module.exports = router;